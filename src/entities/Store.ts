export interface Store {
  storeId: number;
  storeName: string;
  contactNumber: string;
  picture?: string;
  status: string;
}

export interface StoreInfo {
  storeId: number;
  storeName: string;
}
